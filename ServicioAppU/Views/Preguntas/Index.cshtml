@model IEnumerable<ServicioAppU.ModelosNuevos.Preguntas>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Descripcion)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.IdCodigoNavigation.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCodigoNavigation.Descripcion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdPreguntas">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdPreguntas">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdPreguntas">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
